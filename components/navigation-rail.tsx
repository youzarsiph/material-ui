import Props, { State } from "./interfaces";
import { Component, ReactNode } from "react";
import styles from "../styles/components/NavigationRail.module.css";

interface NavigationRailProps extends Props {}

class NavigationRail extends Component<NavigationRailProps, State> {
  render(): ReactNode {
    return <div></div>;
  }
}
